name: get-and-notify

on:
  # schedule:
  #   - cron: '0 18 * * *'
  workflow_dispatch:
    inputs:

jobs:
  get-info-and-send:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: set up python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      # - name: install dependencies
      #   run: |
      #     python -m pip install --upgrade pip
      #     pip install discord-webhook requests
      - name: run script
        run: python -m main
        env:
          DEV_TMIO_API_BASE: ${{ env.DEV_TMIO_API_BASE }}